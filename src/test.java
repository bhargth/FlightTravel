public class test {
    public static void main(String[] args) {

        double amount = 0;
        amount += 0.1; // Same thing as: amount = amount + 0.1f
        amount += 0.1;
        amount += 0.1;
        amount += 0.1;
        amount += 0.1;
        amount += 0.1;
        amount += 0.1;
        amount += 0.1;
        amount += 0.1;
        amount += 0.1;
        System.out.println(amount);
    }
}
